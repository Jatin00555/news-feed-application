export interface ToggleElementType {
  key: string | number;
  label: string;
}

export interface NewsArticleType {
  avatarUrl: string;
  title: string;
  subheader: string;
  imageUrl: string;
  description: string;
}
